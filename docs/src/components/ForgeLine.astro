<div class="mx-auto h-px w-40 sm:w-56" aria-hidden="true">
  <div class="forge-line h-full w-full rounded-full" />
</div>

<style>
  .forge-line {
    background: linear-gradient(90deg, transparent 0%, oklch(0.70 0.14 50 / 0.3) 20%, oklch(0.78 0.18 65) 45%, oklch(0.85 0.16 80) 55%, oklch(0.70 0.14 50 / 0.3) 80%, transparent 100%);
    animation: heat-pulse 4s ease-in-out infinite;
  }
  @keyframes heat-pulse {
    0%, 100% { opacity: 0.7; filter: brightness(1); }
    50% { opacity: 1; filter: brightness(1.2); }
  }
  @media (prefers-reduced-motion: reduce) {
    .forge-line { animation: none; opacity: 0.85; }
  }
</style>
